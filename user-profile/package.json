{
  "name": "set-permissions",
  "version": "1.0.0",
  "description": "This Lambda function will set permissions to make transcoded videos publically available",
  "main": "index.js",
  "scripts": {
    "deploy": "aws lambda update-function-code --function-name  arn:aws:lambda:us-east-1:653810806518:function:user-profile --zip-file fileb://Lambda-Deployment.zip",
    "predeploy": "zip -r Lambda-Deployment.zip * -x *.zip *.json *.log"
  },
  "author": "Peter Sbarski",
  "license": "BSD-2-Clause",
  "dependencies": {
    "jsonwebtoken": "^8.3.0",
    "request": "^2.88.0"
  }
}
